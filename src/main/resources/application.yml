application:
  name: ${APPLICATION_NAME}
spring:
  data:
    web:
      pageable:
        page-serialization-mode: ${SPRING_DATA_WEB_PAGE_SERIALIZATION_MODE}
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      password: ${SPRING_DATA_REDIS_PASSWORD:}
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
      pool-name: AbrolhosHikariPool
  flyway:
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE}

management:
  endpoints:
    web:
      exposure:
        include: ${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE:health,info,metrics,prometheus}
  endpoint:
    health:
      probes:
        enabled: ${MANAGEMENT_ENDPOINTS_HEALTH_PROBES_ENABLED}
      show-details: always
      group:
        readiness:
          include: readinessState,db,redis
        liveness:
          include: livenessState,diskSpace
  metrics:
    tags:
      application: abrolhos
      environment: ${ENVIRONMENT:dev}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}

jwt:
  secret: ${JWT_SECRET}
  expiration-hours: ${JWT_EXPIRATION_HOURS:24}

rate-limit:
  auth:
    max-attempts: ${RATE_LIMIT_AUTH_MAX_ATTEMPTS:5}
    window-seconds: ${RATE_LIMIT_AUTH_WINDOW_SECONDS:300}

# Security hardening configuration
security:
  cors:
    allowed-origins: ${SECURITY_CORS_ALLOWED_ORIGINS:${CORS_ALLOWED_ORIGINS}}
  rate-limit:
    max-requests: ${SECURITY_RATE_LIMIT_MAX_REQUESTS:5}
    window-minutes: ${SECURITY_RATE_LIMIT_WINDOW_MINUTES:1}
  encryption:
    key: ${SECURITY_ENCRYPTION_KEY:}
    old-keys: ${SECURITY_ENCRYPTION_OLD_KEYS:}

logging:
  level:
    root: INFO
    br.dev.demoraes.abrolhos: DEBUG

server:
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/plain
    min-response-size: 1024
